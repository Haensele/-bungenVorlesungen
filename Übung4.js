const names = ["Lukas", "Noah", "Mario", "Kevin"];

const mappedNames =names.map((name) => name.toUpperCase());
console.log(mappedNames); 

names.push("Stefan");

console.log(names);

names.pop();

console.log(names);

names.reverse();

console.log(names);

